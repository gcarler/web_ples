// This component has been temporarily emptied to resolve build issues.
// It can be restored when the underlying dependencies are resolved.
import * as React from "react"

export function DataTableFacetedFilter() {
  return null;
}
